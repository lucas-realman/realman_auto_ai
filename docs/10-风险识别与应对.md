# 睿尔曼智能 CRM — 风险识别与应对

> **版本**: v1.0
> **日期**: 2026-03-04
> **依赖文档**: 01~09 全部文档交叉审查
> **定位**: 识别系统方案、硬件、测试、开发计划、自动化流水线中的潜在问题和风险，给出应对措施与行动计划
> **审查方法**: 逐文档精读 → 交叉比对 → 分级分类 → 给出行动项

---

## 目录

1. [风险分级标准](#1-风险分级标准)
2. [P0 阻塞级问题（必须立即处理）](#2-p0-阻塞级问题必须立即处理)
3. [P1 高风险问题（需在开发前解决）](#3-p1-高风险问题需在开发前解决)
4. [P2 中风险问题（需要关注的设计问题）](#4-p2-中风险问题需要关注的设计问题)
5. [P3 优化建议](#5-p3-优化建议)
6. [文档间交叉一致性问题](#6-文档间交叉一致性问题)
7. [优先行动计划与跟踪表](#7-优先行动计划与跟踪表)

---

## 1. 风险分级标准

| 级别 | 含义 | 响应时限 | 颜色 |
|------|------|---------|------|
| **P0 阻塞** | 直接阻塞开发或导致安全事故，不修复无法启动 Sprint | **立即** | 🔴 |
| **P1 高风险** | 不处理将在 1-2 个 Sprint 内暴发，导致返工或目标无法达成 | **S1 开始前** | 🟠 |
| **P2 中风险** | 当前不阻塞但会影响系统健壮性或运维效率 | **本 Sprint 内** | 🟡 |
| **P3 优化** | 改进建议，提升效率或质量 | **排入 Backlog** | 🟢 |

---

## 2. P0 阻塞级问题（必须立即处理）

### R-001 🔴 API Key / 凭据明文硬编码在设计文档中

| 维度 | 内容 |
|------|------|
| **涉及文档** | 08-自动化开发流水线.md (§1.5, §2.5, §3.2, §13.5, dispatcher 源码 共 5+ 处)、03-硬件清单.md (6 台机器登录密码)、09-测试机搭建计划.md (§2.3 PG 密码 `test_pass`) |
| **风险描述** | yibuapi.com 的 API Key 在 08 文档中以明文出现 5+ 次（已修复）。03 文档含全部 6 台机器的 SSH 登录密码（已修复）。09 文档含数据库密码（已修复）。这些凭据已迁移到 `.env` 文件，文档中替换为 `${变量名}` 占位符。 |
| **影响范围** | 全部 6 台机器的安全性、Claude Opus 4.6 编码 API 的资金安全、测试数据库安全 |
| **发生概率** | 高 — Git 仓库是团队协作的核心，扩展人员或迁移仓库时极易泄漏 |
| **应对措施** | 1. 所有凭据替换为环境变量引用（如 `${OPENAI_API_KEY}`、`${PASSWORD_ORCHESTRATOR}`）✅<br>2. 各机器的实际凭据写入 `.env` 文件，不入 Git✅<br>3. `.env.example` 提供模板✅<br>4. `.gitignore` 排除 `.env`✅ |
| **责任人** | 项目负责人 |
| **截止日期** | S1 Day 1 —— 已完成 |
| **状态** | 🟢 已解决（2026-03-04） |

### R-002 🔴 模型文件缺失 — Qwen3-30B-A3B 不完整

| 维度 | 内容 |
|------|------|
| **涉及文档** | 04-系统概要设计.md §7.1、08 §1.4 (vLLM A/B/C 均依赖此模型) |
| **风险描述** | `models/Qwen3-30B-A3B/` 目录下共 16 个分片文件（model-00001 至 model-00016），其中 `model-00011-of-00016.safetensors` 缺失。vLLM 加载需要全部分片，缺失任何一个都将报错无法启动，**直接阻塞 vLLM A (4090)、vLLM B (orchestrator)、vLLM C (aliyun-agent) 三个实例的部署**。 |
| **影响范围** | Agent 推理（vLLM A）、AutoReview + 测试（vLLM B）、Staging 验收（vLLM C）全部不可用 |
| **发生概率** | 已确认 — 当前文件系统中缺失该分片 |
| **应对措施** | 1. 立即从 HuggingFace 或原始源重新下载缺失分片：<br>`huggingface-cli download Qwen/Qwen3-30B-A3B model-00011-of-00016.safetensors --local-dir ~/models/Qwen3-30B-A3B/`<br>2. 下载完成后校验完整性：对照 `model.safetensors.index.json` 中的 weight_map 确认全部 16 个分片存在<br>3. 同步到 aliyun-agent（rsync / huggingface-cli 直接下载） |
| **责任人** | 项目负责人 |
| **截止日期** | 立即 |
| **状态** | 🔴 待处理 |

### R-003 🔴 自举悖论 — Orchestrator 自身无法被自动化开发

| 维度 | 内容 |
|------|------|
| **涉及文档** | 08-自动化开发流水线.md 全文、07-Sprint任务卡.md S1-2 W0 列 |
| **风险描述** | 全自动开发流水线的核心是 Orchestrator（解析文档→生成任务→SSH分发→Review→判定），但 **Orchestrator 本身需要人工编码**。07 任务卡 S1-2 的 W0 列清晰列出了手动搭建 Orchestrator 的逐日任务。这意味着：(1) 前 2 个 Sprint 无法享受自动化红利，完全依赖手工编码；(2) Orchestrator 的质量完全取决于手工编码水平，没有自动 Review 保障；(3) 如果 Orchestrator 自身有 bug，将系统性地向下游传播错误代码。 |
| **影响范围** | 自动化流水线可靠性、S1-2 开发效率、全项目质量基线 |
| **发生概率** | 必然 — 这是架构固有的循环依赖 |
| **应对措施** | 1. **明确 S1-2 为手工模式**，不假装自动化，在 06/07 文档中标注 "S1-2 = 手工开发 + 手工 Review"<br>2. **Orchestrator 自身要有独立的测试套件**：在 S1 编写时同步编写 `tests/orchestrator/` 单元测试<br>3. **渐进升级路径**：S1-2 手工 → S3-4 半自动（Orchestrator 辅助调度，人工 Review）→ S5+ 全自动<br>4. Orchestrator 代码在 S2 末尾做一次人工专项 Review |
| **责任人** | 项目负责人 |
| **截止日期** | S1 Day 1 前确认策略 |
| **状态** | 🔴 待处理 |

---

## 3. P1 高风险问题（需在开发前解决）

### R-004 🟠 05-测试方案大量内容未与架构迁移同步

| 维度 | 内容 |
|------|------|
| **涉及文档** | 05-测试方案与计划.md §5.1/§5.2/§5.3/§5.4/§6.1/§6.3/§7.1/§7.2 |
| **风险描述** | 虽然 05 §2 已声明"测试迁移到 orchestrator 本地执行"，但后续大量章节仍引用旧 "L20" 架构：§5.1 测试金字塔写"L20 自动跑"、§5.2-5.4 运行位置写"测试机 L20"、§6.1 流水线图触发 SSH 到"测试机 (L20)"、§6.3 test-runner 注释"测试机 L20 上运行"、§7.1 测试环境表列"测试环境 (L20)"、§7.2 locust 拓扑写"测试机 L20 (云端)"。 |
| **影响范围** | Orchestrator 的 DocParser 读取这些章节时会产生矛盾信息；执行团队可能将测试环境搭错位置 |
| **发生概率** | 高 — DocParser 依赖文档文字做任务拆解，矛盾信息直接导致错误任务 |
| **应对措施** | 批量搜索替换 05 文档中的"L20"引用，统一改为 orchestrator 本地执行。保留 aliyun-agent 作为 Staging 的描述。具体替换清单见§6。 |
| **责任人** | 项目负责人 |
| **截止日期** | S1 前 |
| **状态** | 🟠 待处理 |

### R-005 🟠 单人团队 + 6 机器运维 + 4 周交付 = 极高执行风险

| 维度 | 内容 |
|------|------|
| **涉及文档** | 06-总体开发计划.md、07-Sprint任务卡.md、03-硬件清单.md |
| **风险描述** | 项目为 1 人团队，需同时管理：(1) 6 台机器的硬件运维（SSH、GPU 驱动、防火墙、磁盘空间）；(2) 庞大的软件栈（PostgreSQL + Redis + vLLM×3 + Nginx + Celery + Prometheus + Grafana + aider×5）；(3) 一期开发产出（6 Agent + 11 Tool + 4 REST API 组 + 6 Vue3 页面 + 300+ 测试）；(4) 9 份现有文档维护 + 4 份待编写新文档；(5) 外部依赖协调（钉钉 AppKey、3 个云端 Judge API Key、yibuapi.com 可用性）。任何一台机器的故障都会阻塞开发。 |
| **影响范围** | 项目整体交付时间线 |
| **发生概率** | 高 — 6 台异构机器的运维本身就需要大量时间 |
| **应对措施** | 1. **砍一期非核心功能**：Vue3 看板可降级为极简单页面（1-2 页而非 6 页）<br>2. **钉钉互动卡片 / 审批流推到二期**，一期只做机器人对话<br>3. 接受"Agent 不完美但可用"的质量基线，Prompt 调优不追求完美<br>4. **优先搭建 Orchestrator + 自动化**，让机器分担编码工作量<br>5. 准备"功能降级交付矩阵"：核心 3 Agent（Supervisor + 销售助手 + 线索评分）必保，其余 3 个 Agent 视进度取舍 |
| **责任人** | 项目负责人 |
| **截止日期** | S1 前确认砍减方案 |
| **状态** | 🟠 待处理 |

### R-006 🟠 第三方 API 网关（yibuapi.com）单点依赖

| 维度 | 内容 |
|------|------|
| **涉及文档** | 08-自动化开发流水线.md §1.5、§3.2 |
| **风险描述** | 全部 5 台机器的 aider 编码统一通过 yibuapi.com 转发 Claude Opus 4.6。该第三方：(1) 非 Anthropic 官方，无 SLA 保证；(2) 可能随时调价、限流或下线；(3) 如果不可用，**所有自动化编码停摆**。编码是流水线最核心的环节，如果停摆将导致每日 50-100 个任务归零。 |
| **影响范围** | 全部 5 台机器的自动化编码能力 |
| **发生概率** | 中 — 第三方服务中断概率不低，尤其在促销/维护时 |
| **应对措施** | 1. **准备 fallback 编码模型配置**：本地 Qwen3-30B-A3B + aider（效果弱于 Claude 但可兜底），或备用 API 网关<br>2. **估算月成本并设预警**：50-100 task/day × ~4K tokens/task × Claude 单价 ≈ 每月 $XXX，设置消费上限<br>3. 在 Orchestrator 的 dispatcher 中实现**自动降级**：yibuapi.com 连续 3 次超时 → 切本地 vLLM 编码<br>4. 考虑同时配置一个备用 Claude API 供应商 |
| **责任人** | 项目负责人 |
| **截止日期** | S1 前 |
| **状态** | 🟠 待处理 |

### R-007 🟠 aliyun-agent 到内网的网络连通性未确认

| 维度 | 内容 |
|------|------|
| **涉及文档** | 09-测试机搭建计划.md §2.3 (SSH 到 mac_min_8T)、05-测试方案 §7.2 (locust 从云端压测内网)、03-硬件清单.md §6 网络拓扑 |
| **风险描述** | 多处功能假设云端 aliyun-agent 可以 SSH 到内网机器：(1) Staging 代码同步需要 `ssh edge_sale@172.16.12.50`；(2) locust 从 aliyun-agent 压测内网 gateway；(3) Staging 验收需要 Agent 引擎连接内网数据库。但 172.16.x.x 是私有地址，从公网无法直达。**如果没有 VPN 或穿透方案，整个 Staging 环境形同虚设。** |
| **影响范围** | Staging 预发布验收、性能压测、代码自动同步 |
| **发生概率** | 高 — 文档中没有任何 VPN / 穿透 / 公网映射的描述 |
| **应对措施** | 1. **确认网络方案**：VPN（推荐 WireGuard，轻量）/ FRP 内网穿透 / 公网端口映射，三选一<br>2. 将选定方案写入 03 或 09 文档<br>3. 如果短期无法打通，将 Staging 验收改为在 orchestrator 上用独立数据库模拟，推迟 aliyun-agent 的使用 |
| **责任人** | 项目负责人 |
| **截止日期** | S1 前确认方案 |
| **状态** | 🟠 待处理 |

### R-008 🟠 Embedding 模型未规划部署

| 维度 | 内容 |
|------|------|
| **涉及文档** | 04-系统概要设计.md §7.1 (选型 BGE-M3 或 Qwen-Embedding)、§8.3 Knowledge 表 (vector(1024)) |
| **风险描述** | 04 §7.1 在技术选型中明确提到使用 BGE-M3 或 Qwen-Embedding 做向量嵌入，pgvector 的知识库检索（§8.3 Knowledge 表）依赖此模型。但：(1) 整个 07 Sprint 任务卡中**没有 Embedding 模型部署任务**；(2) 不清楚 Embedding 在哪台机器跑；(3) 不清楚是否与 vLLM 共享 GPU（BGE-M3 约需 2-4GB 显存）。 |
| **影响范围** | 知识库语义检索功能、客户洞察 Agent 的 RAG 能力 |
| **发生概率** | 必然 — 任务卡中缺失此项 |
| **应对措施** | 1. 在 S5-6（知识检索功能入场时）前明确 Embedding 部署方案<br>2. **推荐方案**：使用云端 Embedding API（阿里云 / OpenAI）降低本地 GPU 压力，一期知识量不大，成本极低<br>3. 如选本地部署，在 4090 或 orchestrator 上与 vLLM 分时加载 |
| **责任人** | 项目负责人 |
| **截止日期** | S5 前 |
| **状态** | 🟠 待处理 |

### R-009 🟠 AutoReview 用 3B 激活参数模型审查 Claude Opus 生成的代码

| 维度 | 内容 |
|------|------|
| **涉及文档** | 08-自动化开发流水线.md §4.1-4.2 (三层自动 Review)、04 §7.1 (Qwen3-30B-A3B, MoE 激活3B) |
| **风险描述** | vLLM B 跑的是 Qwen3-30B-A3B（MoE 架构，实际激活仅 3B 参数），而编码使用的是 Claude Opus 4.6（顶级模型）。导致：(1) **用远弱于编码模型的模型来审查代码**，Layer 2 契约对齐和 Layer 3 设计符合度的判断能力有限；(2) 可能出现"审查通过但实际有问题"（漏判）或"误判导致无限重试"（误报，对 3 次重试资源浪费严重）；(3) 08 §4.4 的修复指令生成质量也受限于 3B 模型能力。 |
| **影响范围** | 自动 Review 的有效性、代码质量保证、自动重试的效率 |
| **发生概率** | 高 — 3B 模型对复杂 Python 逻辑的理解力确实有限 |
| **应对措施** | 1. **Review Layer 2/3 也走 yibuapi.com / Claude API**（Review 频率低于编码，成本增加可控）<br>2. 或在 orchestrator 上部署 Qwen3.5-35B-A3B（workspace 中已有模型文件，能力更强）<br>3. 折中方案：Layer 2 用本地 vLLM（接口签名对比相对简单），Layer 3 走云端（复杂逻辑判断需要强模型） |
| **责任人** | 项目负责人 |
| **截止日期** | S1 前确认策略 |
| **状态** | 🟠 待处理 |

### R-010 🟠 钉钉集成前置条件未确认

| 维度 | 内容 |
|------|------|
| **涉及文档** | 04-系统概要设计.md §14 待确认清单 #2、§7.5 钉钉集成选型 |
| **风险描述** | 04 §14 明确列出"钉钉应用已注册？有 AppKey/AppSecret？"为**待确认项**。钉钉是一期的主交互入口（04 §7.4），S3-4 就需要钉钉机器人对话联调。钉钉开发者应用注册需要：(1) 企业管理员审批（可能需要数天）；(2) 配置回调 URL（需要公网可达的 gateway IP 或域名）；(3) IP 白名单配置；(4) 测试环境的机器人需要单独创建。 |
| **影响范围** | S3-4 钉钉联调（关键路径） |
| **发生概率** | 高 — 审批流程不受控 |
| **应对措施** | 1. **S1 Day 1 立即启动钉钉应用注册**<br>2. 同步确认 gateway 的公网访问方案（域名 or IP + 端口映射）<br>3. 如果审批耗时过长，S3-4 先用 Mock 钉钉回调进行开发，真实联调推迟到 S5 |
| **责任人** | 项目负责人 |
| **截止日期** | S1 Day 1 发起，S3 前完成 |
| **状态** | 🟠 待处理 |

### R-011 🟠 4 份关键文档尚未编写

| 维度 | 内容 |
|------|------|
| **涉及文档** | 04-系统概要设计.md 附录 A 文档规划 |
| **风险描述** | 以下 4 份文档标记为"待编写"，但它们是后续 Sprint 的**阻塞依赖**：(1) **Agent 详细设计**（10）— S3-4 开始写 Agent 前必须有 Prompt 设计、Tool 定义、Agent 间协议；(2) **数据库设计**（11）— S1-2 创建 Schema 需要完整表结构；(3) **钉钉集成方案**（12）— S3-4 联调需要方案；(4) **部署方案**（13）— S7-8 切运行模式需要 docker-compose.yml 等。 |
| **影响范围** | S1-2 Schema 创建、S3-4 Agent 开发和钉钉联调、S7-8 部署 |
| **发生概率** | 必然 — 目前为空白 |
| **应对措施** | 1. **数据库设计**：S1 Day 2 前完成（阻塞 Schema 创建）<br>2. **Agent 详细设计**：S2 末尾前完成（阻塞 S3-4 Agent 编码）<br>3. **钉钉集成方案**：S2 末尾前完成（阻塞 S3-4 联调）<br>4. **部署方案**：S6 末尾前完成（阻塞 S7-8 上线切换） |
| **责任人** | 项目负责人 |
| **截止日期** | 见上述各子项 |
| **状态** | 🟠 待处理 |

---

## 4. P2 中等风险问题（需在相关 Sprint 前解决）

### R-012 🟡 mac_min_8T 角色过载

| 维度 | 内容 |
|------|------|
| **涉及文档** | 03-硬件清单.md §2、04-系统概要设计.md §5、08-自动化开发流水线.md §2.4 |
| **风险描述** | mac_min_8T (M4 Pro 64GB) 同时承担：(1) PostgreSQL 数据库、(2) Ollama Embedding 推理、(3) Redis 缓存、(4) Celery Worker、(5) 前端 Vue3 开发构建、(6) macOS 暴露率高于 Linux 的稳定性风险。问题在于 macOS 不支持 Docker GPU 透传（Metal 不兼容），未来 Docker 化时需要特殊处理。 |
| **影响范围** | 数据库可用性、前端构建效率 |
| **发生概率** | 中 — 64GB 内存理论足够，但 macOS 长时间运行稳定性一般 |
| **应对措施** | 1. PostgreSQL 启用 WAL 归档 + 定期 pg_dump 到 orchestrator<br>2. 考虑将 Redis 迁移到 orchestrator（128GB 内存更宽裕）<br>3. 设置 macOS 自动重启脚本（launchd），服务崩溃自动恢复 |
| **责任人** | 项目负责人 |
| **截止日期** | S1 中 |
| **状态** | 🟡 待处理 |

### R-013 🟡 Git 分支冲突风险被低估

| 维度 | 内容 |
|------|------|
| **涉及文档** | 08-自动化开发流水线.md §3.4 分支策略、§5.3 冲突处理 |
| **风险描述** | 08 §3.4 设计了多机并行编码（同时最多 5 台 aider 提交），虽然有"按 Agent 划分子目录"的策略，但以下共享文件必然冲突：(1) `requirements.txt`（多个 Agent 新增依赖）；(2) `config.py`（Agent 注册配置）；(3) Router 路由注册文件；(4) Docker compose / 部署配置文件。08 §5.3 的冲突处理（"标记 CONFLICT 等待人工"）在无人看守的自动化流程中会导致任务**永久挂起**。 |
| **影响范围** | 多机并行效率、自动化流水线连续性 |
| **发生概率** | 高 — 共享文件冲突在并行开发中不可避免 |
| **应对措施** | 1. **共享文件串行化**：对 `requirements.txt`、`config.py` 等文件加分布式锁（Redis），同一时刻只允许一台机器修改<br>2. 自动冲突处理：简单冲突（如 requirements.txt 追加行）用脚本自动 merge<br>3. 复杂冲突设置超时自动 abort 并通知 Orchestrator 重排任务 |
| **责任人** | 项目负责人 |
| **截止日期** | S2 前 |
| **状态** | 🟡 待处理 |

### R-014 🟡 S1-2 "已完成" 状态不清晰

| 维度 | 内容 |
|------|------|
| **涉及文档** | 06-总体开发计划.md、07-Sprint任务卡.md |
| **风险描述** | 06 和 07 将 S1-2 标记为"✅ 已完成"，但：(1) Orchestrator 本身是否已有可运行的代码？如果没有，S1-2 的"完成"只是文档和环境准备；(2) workspace 中未见 Orchestrator 的代码仓库；(3) 09 §2.5 vLLM 实例 B (orchestrator) 的部署状态未确认。"已完成"是需要重新验证的。 |
| **影响范围** | S3-4 的启动条件 |
| **发生概率** | 中 — 可能只是文档完成而非软件环境 ready |
| **应对措施** | 1. 创建 S1-2 **交付物检查清单**：Git Bare Repo 可用？vLLM A/B 可服务？PostgreSQL 可连接？aider 可编码？<br>2. 逐项验证，未通过项补入 S3 前置任务<br>3. 将检查清单结果记入本文档附录 |
| **责任人** | 项目负责人 |
| **截止日期** | S3 Day 1 前 |
| **状态** | 🟡 待处理 |

### R-015 🟡 Redis 无持久化配置

| 维度 | 内容 |
|------|------|
| **涉及文档** | 04-系统概要设计.md §7.3、09-测试机搭建计划.md |
| **风险描述** | 09 中 Redis 部署步骤未配置 RDB/AOF 持久化。Redis 同时承担：(1) Celery Broker（任务队列）、(2) Agent 会话上下文缓存、(3) Orchestrator 任务调度状态。一旦 Redis 进程重启或 OOM，**所有进行中的 Celery 任务丢失**，Orchestrator 的任务状态也会断裂。 |
| **影响范围** | 任务队列可靠性、会话连续性 |
| **发生概率** | 中 — macOS 上 Redis 长期运行有内存泄漏风险 |
| **应对措施** | 1. 至少开启 `appendonly yes` (AOF) 持久化<br>2. Celery 建议配置 `task_acks_late=True` + `worker_prefetch_multiplier=1` 防止任务丢失<br>3. 关键状态同时写入 PostgreSQL（双写），Redis 作为加速层而非唯一存储 |
| **责任人** | 项目负责人 |
| **截止日期** | S1 中 |
| **状态** | 🟡 待处理 |

### R-016 🟡 Git Bare Repo 无备份

| 维度 | 内容 |
|------|------|
| **涉及文档** | 08-自动化开发流水线.md §3.1、09-测试机搭建计划.md §2.2 |
| **风险描述** | Git Bare Repo 位于 gateway 单机上（09 §2.2），是全部代码和自动化流水线的**唯一源头**。gateway 配置较低（03 §2 未详述硬件），一旦硬盘故障，全部代码丢失。目前没有任何备份或镜像策略。 |
| **影响范围** | 全部代码和开发历史 |
| **发生概率** | 低 — 但后果极其严重（灾难性） |
| **应对措施** | 1. 每日 cron 将 bare repo `git clone --mirror` 到 orchestrator<br>2. 每周推送到 GitHub/GitLab 私有仓库做异地备份<br>3. 在 post-receive hook 中增加备份触发 |
| **责任人** | 项目负责人 |
| **截止日期** | S1 中 |
| **状态** | 🟡 待处理 |

### R-017 🟡 Cloud Judge API 成本与可用性风险

| 维度 | 内容 |
|------|------|
| **涉及文档** | 05-测试方案与计划.md §4.5、09-测试机搭建计划.md §4 judge.py |
| **风险描述** | 三模型 Judge（GPT-4.1 40%、Qwen-Max 35%、DeepSeek-V3 25%）用于测试打分：(1) 每个测试用例调 3 个 API，300+ 测试 × 3 = 900+ API 调用 / 次全量测试；(2) 每次 API 调用含长 System Prompt + 测试输出，token 消耗约 2-4K/次；(3) 3 家 API 任一不可用就导致评分不完整；(4) 月成本可能达到 $50-200 取决于测试频率。 |
| **影响范围** | 测试自动化频率、月度成本 |
| **发生概率** | 中 — API 可用性通常较好，但成本可能超预期 |
| **应对措施** | 1. **设置每日测试频率上限**（如：全量测试每日 ≤ 2 次，PR 触发只跑相关模块）<br>2. Judge 结果缓存：相同输入 + 相同评分标准不重复调用<br>3. 准备降级方案：如只用 1 个模型（Qwen-Max，国内最稳定最便宜）做快速测试 |
| **责任人** | 项目负责人 |
| **截止日期** | S3 前 |
| **状态** | 🟡 待处理 |

---

## 5. P3 优化建议（可按优先级逐步实施）

### R-018 🔵 升级 vLLM B 至 Qwen3.5-35B-A3B

| 维度 | 内容 |
|------|------|
| **涉及文档** | 04-系统概要设计.md §7.1、08-自动化开发流水线.md §4.1 |
| **风险描述** | workspace 中已下载 Qwen3.5-35B-A3B 模型文件（`models/Qwen3.5-35B-A3B/`），其能力显著优于当前使用的 Qwen3-30B-A3B。orchestrator 的 2×4090（48GB 总显存）可以跑此模型。升级后 AutoReview 质量（R-009）和 Agent 推理能力均会提升。 |
| **建议** | S2 末尾用 Qwen3.5-35B-A3B 替换 vLLM B，可同步解决 R-009 的弱模型审查问题 |
| **优先级** | 建议 — 非阻塞，但收益显著 |

### R-019 🔵 优先开发 Orchestrator Web Dashboard

| 维度 | 内容 |
|------|------|
| **涉及文档** | 08-自动化开发流水线.md §6 |
| **风险描述** | 目前 Orchestrator 的可观测性为 0 — 无法实时看到任务分配、aider 编码状态、Review 结果、测试报告。08 §6 提到了 Web Dashboard 但标记为"后续迭代"。单人团队运维 6 台机器，**没有仪表盘等于盲飞**。 |
| **建议** | 在 S2 末尾实现最简版 Dashboard：(1) 当前任务列表 + 状态（进行中/成功/失败）；(2) 最近 10 次 Review 评分；(3) vLLM 各实例健康状态。用 FastAPI + 简单 HTML 即可，无需 Vue 框架。 |
| **优先级** | 强烈建议 — 对运维效率提升极大 |

### R-020 🔵 提前准备 Docker Compose

| 维度 | 内容 |
|------|------|
| **涉及文档** | 06-总体开发计划.md S7-8、04-系统概要设计.md §11 |
| **风险描述** | 部署方案（doc 13）列为"待编写"，S7-8 才做 Docker 化。但如果开发期间在裸机上跑，到 S7-8 迁移到 Docker 时可能遇到大量环境差异。 |
| **建议** | S1 就编写基础 docker-compose.yml（PostgreSQL + Redis + 后端），开发期间用 Docker 跑依赖服务，应用本身保持裸机开发。这样 S7-8 只需增加 vLLM 和前端容器。 |
| **优先级** | 建议 — 降低 S7-8 的集成风险 |

### R-021 🔵 增加磁盘空间监控

| 维度 | 内容 |
|------|------|
| **涉及文档** | 08-自动化开发流水线.md §2.5 资源规划、03-硬件清单.md |
| **风险描述** | vLLM 模型文件占据大量磁盘（Qwen3-30B-A3B 约 30GB，3.5-35B 约 28GB），加上 Git 历史、日志文件、PostgreSQL WAL。orchestrator（1TB SSD）和 4090（可用空间未知）可能在 S5-6 后磁盘紧张。 |
| **建议** | 1. 各机器添加 cron `df -h` 检查，<20% 余量时告警<br>2. 定期清理旧 Git 分支和日志文件<br>3. 在 Prometheus 中添加 `node_filesystem_avail_bytes` 告警规则 |
| **优先级** | 建议 |

### R-022 🔵 考虑一期减少 Agent 数量

| 维度 | 内容 |
|------|------|
| **涉及文档** | 04-系统概要设计.md §3.2（14 Agent 规划）、06-总体开发计划.md（一期 6 Agent） |
| **风险描述** | 一期规划 6 个 Agent，每个 Agent 含 Prompt 设计 + Tool 实现 + 测试用例 + 联调。在 R-005 识别的执行风险下，6 个 Agent 可能做不完。 |
| **建议** | 将一期**核心保障**缩到 3 个 Agent：(1) Supervisor（路由必需）、(2) 销售助手（核心价值）、(3) 线索评分（差异化卖点）。其余 3 个（客户洞察、任务管理、知识检索）推到二期或作为 Plus 功能。 |
| **优先级** | 强烈建议 — 与 R-005 联动 |

---

## 6. 跨文档一致性问题

以下问题不影响系统运行，但会导致 Orchestrator 的 DocParser **解析出矛盾指令**，应在 S1 前统一修正。

### C-001 测试执行位置描述不一致

| 维度 | 内容 |
|------|------|
| **涉及文档** | 05-测试方案 §2 vs §5.1-§7.2、08 §5.1、09 §2.4 |
| **问题** | 05 §2 已声明迁移到 orchestrator，但 §5-§7 多处仍写"L20"。08 §5.1 和 09 §2.4 已更新为 orchestrator。 |
| **修正建议** | 批量替换 05 §5-§7 中的 "L20" → "orchestrator 本地"，保留 aliyun-agent 作为 Staging 的引用 |

### C-002 vLLM 实例编号约定不一致

| 维度 | 内容 |
|------|------|
| **涉及文档** | 04 §7.1 vs 08 §2.5 |
| **问题** | 04 §7.1 定义 3 个推理实例但未明确编号（只按端口区分：8000/8001/8002）。08 §2.5 使用 A/B/C 编号（A=4090 生产, B=orchestrator 审查+测试, C=aliyun-agent 预发布）。两处未交叉引用。 |
| **修正建议** | 在 04 §7.1 加入 A/B/C 编号说明，与 08 §2.5 对齐 |

### C-003 post-receive hook 逻辑版本不一致

| 维度 | 内容 |
|------|------|
| **涉及文档** | 05-测试方案 §6.1 vs 09-测试机搭建计划 §2.2 |
| **问题** | 05 §6.1 的 hook 逻辑：`git push → SSH 到"测试机(L20)" → pytest`。09 §2.2 的 hook 逻辑：`git push → webhook → orchestrator 调度`。两个流程完全不同（直接 SSH 执行 vs webhook + orchestrator 调度）。 |
| **修正建议** | 以 09 的 webhook 方案为准（更符合 Orchestrator 架构），更新 05 §6.1 |

### C-004 08 §2.5 YAML 缩进错误

| 维度 | 内容 |
|------|------|
| **涉及文档** | 08-自动化开发流水线.md §2.5 |
| **问题** | §2.5 的 `aider` 配置 YAML 中，某些字段缩进不一致（混用 tab 和空格），可能导致 YAML 解析失败。 |
| **修正建议** | 统一为 2 空格缩进，逐行检查 §2.5 的 YAML 代码块 |

### C-005 04 §6.1 缺少 orchestrator 角色

| 维度 | 内容 |
|------|------|
| **涉及文档** | 04-系统概要设计.md §6.1 部署拓扑 |
| **问题** | 04 §6.1 列出的部署拓扑中包含 4090、mac_min_8T、gateway 等机器角色，但未明确列出 orchestrator 的角色（调度中心 + vLLM B + 测试执行）。orchestrator 是 08 和 09 文档引入的核心节点。 |
| **修正建议** | 在 04 §6.1 的部署表中补充 orchestrator 行：`orchestrator (.14.201) — Xeon 64C+128G+2×4090 — 任务调度、vLLM B、自动测试、AutoReview` |

### C-006 Judge 评分维度不一致

| 维度 | 内容 |
|------|------|
| **涉及文档** | 05-测试方案 §4.5 vs 09-测试机搭建计划 §4 judge.py |
| **问题** | 05 §4.5 定义了 5 个评分维度（准确性、完整性、一致性、响应时间、用户体验），而 09 §4 的 judge.py 代码使用了 4 个维度（准确性、相关性、完整性、专业性）。维度名称和数量不匹配。 |
| **修正建议** | 统一为 09 judge.py 的 4 维度（代码为准），更新 05 §4.5 的文档描述 |

---

## 7. 优先行动计划与跟踪表

### 7.1 S1 前（即刻行动）

| 序号 | 行动项 | 关联风险 | 预计耗时 | 状态 |
|------|--------|----------|----------|------|
| 1 | ✅ 将所有 API Key / 密码迁移到 `.env` 文件，文档中替换为占位符 | R-001 | 2h | ✅ |
| 2 | 修复 Qwen3-30B-A3B 模型缺失 shard 11/16 | R-002 | 1h (下载) | ⬜ |
| 3 | 确认 S1-2 交付物清单（Orchestrator 可运行性验证） | R-003, R-014 | 2h | ⬜ |
| 4 | 批量修正 05 文档中 "L20" 引用 → "orchestrator" | R-004, C-001 | 1h | ⬜ |
| 5 | 确认功能砍减方案（核心 3 Agent vs 6 Agent） | R-005, R-022 | 1h | ⬜ |
| 6 | 准备 yibuapi.com fallback 方案 + 配置备用编码模型 | R-006 | 1h | ⬜ |
| 7 | 确认 aliyun-agent 到内网的网络方案（VPN / FRP / 公网映射） | R-007 | 2h | ⬜ |
| 8 | 发起钉钉开发者应用注册 | R-010 | 0.5h | ⬜ |
| 9 | 确认 AutoReview 模型策略（弱模型 or 云端 API） | R-009 | 0.5h | ⬜ |
| 10 | 修复跨文档一致性问题 C-001 ~ C-006 | C-001~C-006 | 2h | ⬜ |

### 7.2 S1-S2 期间

| 序号 | 行动项 | 关联风险 | 预计耗时 | 状态 |
|------|--------|----------|----------|------|
| 11 | 编写数据库设计文档 | R-011 | 4h | ⬜ |
| 12 | 配置 Redis AOF 持久化 | R-015 | 0.5h | ⬜ |
| 13 | 设置 Git Bare Repo 每日备份 | R-016 | 1h | ⬜ |
| 14 | 基础 docker-compose.yml（PostgreSQL + Redis） | R-020 | 2h | ⬜ |
| 15 | 实现最简 Orchestrator Dashboard | R-019 | 4h | ⬜ |
| 16 | 磁盘空间监控 cron | R-021 | 0.5h | ⬜ |
| 17 | 共享文件分布式锁方案 | R-013 | 2h | ⬜ |

### 7.3 S2 末尾

| 序号 | 行动项 | 关联风险 | 预计耗时 | 状态 |
|------|--------|----------|----------|------|
| 18 | 编写 Agent 详细设计文档 | R-011 | 8h | ⬜ |
| 19 | 编写钉钉集成方案文档 | R-011 | 4h | ⬜ |
| 20 | 升级 vLLM B 至 Qwen3.5-35B-A3B | R-018 | 2h | ⬜ |

### 7.4 S3 前

| 序号 | 行动项 | 关联风险 | 预计耗时 | 状态 |
|------|--------|----------|----------|------|
| 21 | 设置 Cloud Judge API 频率上限 + 成本监控 | R-017 | 1h | ⬜ |
| 22 | 确认钉钉 AppKey/AppSecret 到位 | R-010 | (依赖审批) | ⬜ |

### 7.5 S5 前

| 序号 | 行动项 | 关联风险 | 预计耗时 | 状态 |
|------|--------|----------|----------|------|
| 23 | 确认 Embedding 模型部署方案 | R-008 | 1h | ⬜ |

### 7.6 S6 末尾

| 序号 | 行动项 | 关联风险 | 预计耗时 | 状态 |
|------|--------|----------|----------|------|
| 24 | 编写部署方案文档 | R-011 | 4h | ⬜ |

---

## 8. 风险全景统计

| 等级 | 数量 | 状态 |
|------|------|------|
| 🔴 P0 阻塞 | 3 | 1 已解决 / 2 待处理 |
| 🟠 P1 高风险 | 8 | 0 已解决 / 8 待处理 |
| 🟡 P2 中风险 | 6 | 0 已解决 / 6 待处理 |
| 🔵 P3 建议 | 5 | 0 已解决 / 5 待处理 |
| ⚪ 一致性 | 6 | 0 已修正 / 6 待修正 |
| **合计** | **28** | **1 已解决 / 27 待处理** |

---

> **文档维护说明**：每解决一项风险，请更新对应的"状态"字段（⬜→✅ 或 🔴→🟢），并在行动计划表中标记完成。建议每个 Sprint 结束时对本文档做一次全面 Review。
