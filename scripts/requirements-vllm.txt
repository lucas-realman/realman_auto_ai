# Sirus AI CRM — vLLM inference server dependencies
# Target: 2×RTX 4090, CUDA 12.x, Python 3.11+
#
# Tuning parameters (set in start_vllm.sh):
#   --tensor-parallel-size 2
#   --enable-prefix-caching
#   --gpu-memory-utilization 0.85
#   --max-model-len 8192
#   --max-num-seqs 64
#
# Acceptance criteria:
#   TTFT < 2s, throughput > 20 tok/s

vllm>=0.8.0
huggingface-hub>=0.25.0
torch>=2.4.0
requests>=2.31.0

# For health-check / benchmark / testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
aiofiles>=24.0.0
