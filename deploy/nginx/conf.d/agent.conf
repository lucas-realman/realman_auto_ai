# ============================================================
# Sirus AI CRM — Agent 引擎健康检查（独立 location）
# 契约: contracts/agent-api.yaml / contracts/health-api.yaml
# ============================================================
# Agent 引擎的 /health 端点可通过 /agent/health 访问，
# 路由已在 crm.conf 的 location /agent/ 中统一代理。
# 本文件预留用于后续 Agent 引擎独立 server 块（如需独立端口/域名）。
#
# 示例: 如需在 8100 端口直接暴露（仅内网），可取消注释：
# server {
#     listen       8100;
#     server_name  _;
#
#     location / {
#         proxy_pass http://agent_engine;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_buffering off;
#         proxy_cache off;
#         proxy_http_version 1.1;
#         proxy_set_header Connection "";
#         proxy_read_timeout 120s;
#     }
# }
